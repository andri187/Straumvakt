// Stale widget test from the straumvakt_mock1 scaffold is no longer
// applicable in this project. Replaced with a placeholder; real tests
// land when there's coverage to write.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
