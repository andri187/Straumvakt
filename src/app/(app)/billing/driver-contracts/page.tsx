import { BillingStub } from "../_stub";

export const dynamic = "force-dynamic";
export const metadata = { title: "Billing · Driver contracts" };

export default function DriverContractsPage() {
  return <BillingStub title="Driver contracts" blurb="Per-driver subscriptions and home-charging deals." />;
}
