import { BillingStub } from "../../_stub";

export const dynamic = "force-dynamic";
export const metadata = { title: "Billing · New driver contract" };

export default function NewDriverContractPage() {
  return <BillingStub title="New driver contract" blurb="Create form parked while billing entities are ported." />;
}
