import { BillingStub } from "../../_stub";

export const dynamic = "force-dynamic";
export const metadata = { title: "Billing · New contract" };

export default function NewContractPage() {
  return <BillingStub title="New contract" blurb="Create form parked while billing entities are ported." />;
}
