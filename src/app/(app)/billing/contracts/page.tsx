import { BillingStub } from "../_stub";

export const dynamic = "force-dynamic";
export const metadata = { title: "Billing · Contracts" };

export default function ContractsPage() {
  return <BillingStub title="Contracts" blurb="B2B contracts spanning sites, properties, and orgs." />;
}
