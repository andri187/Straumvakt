import { BillingStub } from "../../_stub";

export const dynamic = "force-dynamic";
export const metadata = { title: "Billing · New cost center" };

export default function NewCostCenterPage() {
  return <BillingStub title="New cost center" blurb="Create form parked while billing entities are ported." />;
}
