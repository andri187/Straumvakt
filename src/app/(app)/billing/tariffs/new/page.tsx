import { BillingStub } from "../../_stub";

export const dynamic = "force-dynamic";
export const metadata = { title: "Billing · New tariff" };

export default function NewTariffPage() {
  return <BillingStub title="New tariff" blurb="Create form parked while billing entities are ported." />;
}
