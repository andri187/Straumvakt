{
  "name": "straumvakt-ocpp",
  "version": "0.1.0",
  "private": true,
  "description": "Straumvakt OCPP gateway Worker — WebSocket ingress from chargers, Durable Objects per OCPPIdentity, Service Binding to main app.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260420.0",
    "typescript": "^5.6.3",
    "vitest": "^4.1.5",
    "wrangler": "^3.80.0"
  }
}
